{
    "version": 2,
    "build": {
      "env": {
        "PIP_NO_CACHE_DIR": "1"
      },
      "commands": {
        "install": "pip install --no-cache-dir -r requirements.txt"
      }
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/app.py"
      }
    ]
  }
  